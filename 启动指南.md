# 启动脚本使用指南

## 📁 可用的启动脚本

### 1. start-simple.bat（推荐 ⭐）
**最简单易用的启动脚本**

- ✅ 一键启动所有服务
- ✅ 自动打开 Chrome（Profile 4 工作区）
- ✅ 自动登录题库管理界面
- ✅ 服务器后台运行
- ✅ 不会阻塞命令行窗口

**使用方法：**
```
双击 start-simple.bat
```

### 2. start.py（跨平台）
**Python 版本，支持所有操作系统**

- ✅ Windows/macOS/Linux 通用
- ✅ 智能检测 Chrome 路径
- ✅ 优雅退出（Ctrl+C）
- ✅ 详细的启动日志

**使用方法：**
```bash
python start.py
```

### 3. stop.bat
**停止题库服务器**

- ✅ 一键停止后台服务器
- ✅ 自动查找并关闭进程

**使用方法：**
```
双击 stop.bat
```

## 🚀 快速开始（3 步完成）

### 第 1 步：启动服务
双击运行 **start-simple.bat**

脚本会自动：
1. 启动题库服务器（端口 8002）
2. 打开题库管理界面（自动登录）
3. 打开学习通界面

### 第 2 步：安装用户脚本
1. 确保已安装 [Tampermonkey](https://www.tampermonkey.net/)
2. 打开 `学习通脚本.js` 文件
3. 在 Tampermonkey 中创建新脚本
4. 粘贴内容并保存

### 第 3 步：配置题库地址
在 Tampermonkey 脚本设置中：
- 启用"自定义题库"
- 填入题库地址：`http://localhost:8002/api/search`

## ⚙️ 配置说明

### Chrome 工作区配置
默认使用：**Profile 4**（jiahao071016001@gmail.com）

如需修改，编辑脚本中的：
```batch
set "CHROME_PROFILE=Profile 4"
```

### 端口配置
默认端口：**8002**

如需修改，编辑脚本中的：
```batch
set "TIKU_PORT=8002"
```

### 题库管理界面登录
- 用户名：`admin`
- 密码：`admin`

## 📋 启动后的窗口说明

运行 start-simple.bat 后会看到：

```
========================================
超星学习通自动化系统启动脚本
========================================

[1/3] 启动题库服务器...
正在启动服务器 (端口 8002)...
等待服务器就绪...

[2/3] 打开题库管理界面...

[3/3] 打开学习通界面...

========================================
启动完成！
========================================

服务信息:
  题库管理: http://localhost:8002
  API 接口: http://localhost:8002/api/search
  Chrome 配置: Profile 4 (jiahao071016001@gmail.com)

题库管理界面登录信息:
  用户名: admin
  密码: admin

Tampermonkey 配置:
  在脚本设置中填入: http://localhost:8002/api/search

提示: 服务器在后台运行中，关闭此窗口不会停止服务器
      如需停止服务器，请运行 stop.bat

请按任意键继续. . .
```

## 🌐 Chrome 工作区说明

### 什么是 Chrome 工作区（Profile）？
Chrome 的每个工作区都有独立的：
- 登录账号
- 书签
- 扩展程序
- Cookie 和缓存

### 如何查看当前工作区名称？
1. 打开 Chrome
2. 访问：`chrome://version/`
3. 查看"个人资料路径"，例如：
   ```
   C:\Users\...\Chrome\User Data\Profile 4
   ```
   其中 **Profile 4** 就是工作区名称

### 常见的工作区名称
- `Default` - 默认工作区
- `Profile 1`, `Profile 2`, `Profile 3`... - 其他工作区

## 🔧 故障排除

### 问题 1：服务器启动失败
**现象**：无法访问 http://localhost:8002

**解决方法**：
1. 检查虚拟环境是否存在
   ```
   dir tiku\venv\Scripts\python.exe
   ```
2. 检查依赖是否安装
   ```
   cd tiku
   venv\Scripts\pip.exe list
   ```
3. 手动启动服务器查看错误
   ```
   cd tiku
   venv\Scripts\python.exe main.py
   ```

### 问题 2：Chrome 没有打开正确的工作区
**现象**：打开的不是 jiahao071016001@gmail.com 账号

**解决方法**：
1. 访问 `chrome://version/` 查看正确的 Profile 名称
2. 修改脚本中的 `CHROME_PROFILE` 变量
3. 重新运行启动脚本

### 问题 3：端口 8002 被占用
**现象**：提示端口已被使用

**解决方法**：
1. 运行 `stop.bat` 停止之前的服务器
2. 或者修改 `TIKU_PORT` 为其他端口（如 8003）

### 问题 4：题库管理界面未自动登录
**现象**：打开管理界面后仍显示登录页面

**解决方法**：
1. 手动输入 admin/admin 登录
2. 或者确保前端已重新构建：
   ```
   cd tiku/frontend
   npm run build
   cd ..
   xcopy /E /Y frontend\dist\* static\
   ```

## 📝 使用流程

### 日常使用
1. 双击 `start-simple.bat` 启动
2. 在学习通页面做题
3. 脚本自动从题库查询答案
4. 完成后运行 `stop.bat` 停止服务器

### 题库管理
访问 http://localhost:8002 可以：
- 查看所有题目
- 搜索题目
- 添加新题目
- 导入/导出题库（JSON 格式）
- 查看待处理题目

## 🎯 服务器 API 测试

### 测试服务器是否正常
```bash
curl http://localhost:8002/api/stats
```

应该返回：
```json
{"code":1,"data":{"total":1036,"pending":0}}
```

### 测试题目查询
```bash
curl -X POST http://localhost:8002/api/search ^
  -H "Content-Type: application/json" ^
  -d "{\"question\":\"支撑搭设流程正确的方法是什么？\",\"type\":\"0\"}"
```

应该返回答案：
```json
{"code":-1,"msg":"查询成功","data":{"answer":"A","num":"1036","usenum":"1"}}
```

## 🔄 更新题库

### 方式 1：通过管理界面
1. 访问 http://localhost:8002
2. 点击"导入"按钮
3. 选择 JSON 文件

### 方式 2：直接替换文件
1. 备份当前题库：`copy tiku\tiku.json tiku\tiku.json.bak`
2. 替换新题库文件到 `tiku\tiku.json`
3. 重启服务器

## ⚠️ 注意事项

1. **首次运行**需确保已完成部署：
   - 虚拟环境已创建
   - Python 依赖已安装
   - 前端已构建

2. **Chrome 工作区**必须正确配置，否则可能打开错误的账号

3. **服务器端口**默认 8002，确保没有被其他程序占用

4. **关闭服务器**请使用 `stop.bat`，直接关闭窗口不会停止后台服务

5. **安全提示**：默认无密码保护，仅供本地使用，不要暴露到公网

## 📚 相关文档

- [DEPLOYMENT.md](DEPLOYMENT.md) - 完整部署文档
- [README.md](README.md) - 项目说明
- [README_STARTUP.md](README_STARTUP.md) - 详细启动说明

---

**最后更新**: 2026-09-23  
**Chrome 工作区**: Profile 4 (jiahao071016001@gmail.com)  
**默认端口**: 8002
