# 插件瘦身方案：把可脱离页面的逻辑迁移到本地后端

> 审查快照：`学习通脚本.js`，2026-09-24，2,509 行、173,312 B，SHA-256 为 `DFA74F3AAE8ADC7A50543769CCACB90CFA9CAF1EF04C6D69225784EBD29FFEFC`。
>
> 当前后端：`tiku/`，FastAPI，默认端口 `8002`。
>
> 本文是实施方案，不代表接口已经存在。方案中的“现有”只指当前代码已经确认的能力；“新增”必须完成开发和验收后才能使用。

## 0. 结论

可以把题库查询、AI/Jev 决策、字体字形计算和配置页面逐步迁到本地后端；页面脚本继续负责 DOM、iframe、视频、作业/考试交互。

建议采用“兼容迁移”而不是一次性重写：

1. 先保留当前脚本和 `/api/search`，新增后端能力并做对照验证。
2. 后端确认结果正确后，再让脚本按能力逐项切换。
3. 每完成一项，保留一个可回退开关；全部验收通过后再删除旧依赖。

## 1. 当前代码事实

### 已确认

| 项目 | 当前状态 | 依据 |
|---|---|---|
| 后端题库接口 | 已有 `POST /api/search`，请求包含 `question`、`type`、`options`、可选 `key` | `tiku/app/routes/search.py`、`tiku/app/schemas.py` |
| 后端匹配逻辑 | `database.find_by_question()` 已包含精确、模糊、标准化和选项匹配 | `tiku/app/database.py`、`tiku/app/matching.py` |
| 后端管理页面 | 已有 Vue3/Vite、`/login`、`/questions`、`/pending`，没有 `/settings` | `tiku/frontend/src/router/index.js` |
| 配置存储 | 脚本通过 `GM_getValue("config")` / `GM_setValue("config", ...)` 存储 | `学习通脚本.js` |
| 字体解码 | `decode()` 在页面内读取 `<style>` 和 `.font-cxsecret`，调用 Typr、MD5 和 `table.json` | `学习通脚本.js` |
| 旧题库请求 | `getAnswer` 至 `getAnswer4` 仍是答题兜底；`s()` 还使用旧接口存题 | `学习通脚本.js` |
| 默认端口 | `DEFAULT_PORT = 8002`，启动脚本和脚本配置也使用 8002 | `tiku/app/config.py`、`tiku/start.sh`、`学习通脚本.js` |

### 需要修正的原方案表述

- 原文的基线哈希和字节数与当前文件不一致，已改为本次审查快照；后续修改后应重新生成。
- 当前配置面板不是“184 个字段”，按 `userConfig` 统计约 25 个可编辑字段；应以代码自动统计为准，不要手写固定数字。
- 当前有 8 条 `@require`（包含 `data:` 注入的 Vue 别名），不是 6 条。是否删除要等替代方案验收通过。
- `/api/decode-font`、`/api/answer`、`/settings` 当前都不存在，以下均属于新增目标。
- “每页 1.6 MB”只能作为待测估算，不能作为已验证事实；应以浏览器 Network 和脚本解析耗时实测。

## 2. 迁移边界

### 2.1 适合迁移到后端

- 题库查询、题目标准化、选项匹配和答案格式转换。
- AI prompt、重试、响应解析和 Jev 校验。
- 字体字形解析、路径哈希和 `table.json` 查询。
- 配置的读取、保存、默认值合并和校验。
- 旧题库兜底请求，以及“存题”请求；迁移后必须保留原有成功/失败语义。

### 2.2 必须留在页面脚本

- `Cx.audio/video/work/homework/exam/pdf/s` 的页面控制。
- `getQuestion`、`fillAnswer`、`setAnswer`、`matchAnswer`、`clearCurrent`。
- iframe 和任务点轮询、`videojs`、`UE.getEditor`、页面路由分支。
- 微信扫码弹窗的 `MutationObserver`、剪贴板和 `window.close()`。
- 字体解码的 DOM 两端：提取 `<style>`、替换 `.font-cxsecret` 文本。

## 3. 目标接口与兼容规则

### 3.1 字体：新增 `POST /api/decode-font`

当前流程是页面读取字体、遍历 `19968` 到 `40869` 的字形、计算 `md5(JSON.stringify(path)).slice(24)`，再把结果写回 DOM。后端只承接纯计算，页面仍负责 DOM 替换。

建议请求只发送页面实际出现的编码，避免每次返回两万多个无用映射：

```json
{
  "font_b64": "AAEAAA...",
  "codepoints": [19968, 19969]
}
```

建议响应：

```json
{
  "code": 1,
  "mappings": {"19968": "的", "19969": "一"},
  "missing": []
}
```

实现要求：

1. 先用真实 `font-cxsecret` 样本建立 golden fixture，逐项比对原脚本的映射结果。
2. 不能直接用 Python 重写 Typr；JS 的 `JSON.stringify` 数字格式必须保持一致。
3. 优先评估已有 Node 运行时或可长期复用的 JS worker；确认 Python JS 引擎支持当前 Python 版本后再加入依赖。
4. `table.json` 固定版本并记录来源；下载、许可和完整性校验必须在实施记录中说明。
5. 按字体内容哈希缓存，限制输入大小；缓存命中和缓存未命中都要有测试。

验收：同一个样本在旧脚本和后端得到完全相同的映射，页面文字替换结果一致，后端重启后仍能工作。

### 3.2 答题：优先扩展现有 `POST /api/search`

当前脚本已经请求 `/api/search`，后端也已经实现该接口。为减少一次接口切换，第一版不要强制新增 `/api/answer`；可以在 `/api/search` 内增加服务层，保持现有请求和响应兼容：

- 请求继续接受 `question`、`type`、`options`、`key`。
- 旧客户端继续读取 `data.answer`；新客户端可以额外读取 `source`、`status`、`stage`。
- 未命中仍返回 `code: 0` 并写入待处理题，不能因为增加 AI 兜底而改变原有题库行为。

推荐决策顺序：

1. 本地题库和选项匹配。
2. 旧版题库接口兜底。
3. AI 请求和解析。
4. Jev 校验（仅在启用且 AI 有结果时）。
5. 仍未得到答案时返回明确的空结果，并保留待处理题。

当前实现会并发发起自定义题库和 AI 请求，即使题库已经命中也可能产生一次多余的 AI 请求。迁移时应改为“本地命中即返回、未命中再进入下一阶段”，或实现可取消的并发；不要把无条件并发继续搬到后端。

必须保留的兼容点：

- `getAnswer` 至 `getAnswer4` 的四个旧题库兜底。
- `s()` 的存题能力；可新增 `/api/questions/feedback`，但旧接口未替代前不能删除。
- `parseAIAnswer` 对选择题、判断题、填空题和简答题的现有格式兼容。
- 单次请求超时、重试次数和最终空答案状态，避免页面一直等待。

### 3.3 配置：新增 `/settings` 和配置 API

前端已有 SPA 和登录页，但没有设置页，也没有配置 API。至少需要新增：

- `GET /api/config`：返回脱敏后的配置和版本号。
- `PUT /api/config`：保存经过校验的配置。
- `/settings`：编辑当前 `userConfig` 中的字段。

页面脚本与后端必须约定：配置版本、默认值合并、保存失败处理和后端不可用时的降级行为。迁移初期保留 `GM_getValue/GM_setValue` 作为本地缓存：

- 后端可用：读取后端配置，成功保存后同步本地缓存。
- 后端不可用：继续使用本地配置，页面功能不能整体失效。
- 所有旧配置键都要有迁移映射，未知键保留，避免升级丢设置。

## 4. 目标架构

```mermaid
graph LR
  subgraph Page[页面脚本]
    A[DOM/iframe/视频/作业考试]
    B[轻量请求适配层]
    A --> B
  end
  subgraph BE[localhost:8002]
    C[/api/search 题库+AI+Jev]
    D[/api/decode-font 字体计算]
    E[/api/config 配置读写]
    F[/api/questions/feedback 存题]
  end
  B --> C
  B --> D
  B --> E
  B --> F
```

后端内部按服务拆分：`matching` 负责题库匹配，`answer_service` 负责决策，`font_service` 负责字体，`config_service` 负责配置。接口层只做参数校验和错误转换，避免把第三方请求直接写进路由函数。

## 5. 分阶段实施和验收

| 阶段 | 工作 | 完成标准 | 回退点 |
|---|---|---|---|
| 0 | 建立基线、样本和开关 | 保存脚本哈希；准备字体、选择题、简答题和未命中样本 | 保留当前脚本和 `/api/search` |
| 1 | 抽出答题服务，扩展 `/api/search` | 旧请求结果不变；题库命中不会调用 AI；超时能返回空结果 | 配置开关回到原请求链 |
| 2 | 字体解码实验和 golden fixture | 至少一个真实字体样本逐字节匹配；页面替换结果一致 | 继续页面内 `decode()` |
| 3 | `/api/config` 与 `/settings` | 保存、刷新、重启后配置一致；后端停止时本地配置仍可用 | 继续使用设置弹窗和 GM 存储 |
| 4 | 删除冗余依赖和旧请求 | 浏览视频、音频、作业、考试、扫码检测和答题流程均通过 | 恢复上一版脚本资源列表 |

每阶段都至少做三类验证：

1. 静态检查：`node --check 学习通脚本.js`、Python 编译检查、前端构建。
2. 接口检查：成功、未命中、超时、异常响应、后端重启。
3. 页面回归：章节视频、章节测验、作业、考试、字体混淆题和存题。

## 6. 删除清单（最后执行）

只有在阶段 1 至 4 验收通过后，才考虑删除：

- `Typr`、`blueimp-md5` 和页面内 `decode()` 的纯计算部分。
- AI/Jev/旧题库的页面直连代码和对应 `@connect`。
- 配置面板依赖：Vue、Pinia、ElementPlus、图标、jQuery 以及对应样式资源。

删除前先确认页面脚本仍保留：本地后端请求适配层、GM 配置降级、DOM 替换逻辑和错误提示。不要在同一个提交中同时迁移接口、删除依赖和改动答题策略，这样出现问题时无法定位。

## 7. 风险与待确认

- [ ] 真实字体样本是否能在候选 JS 运行时得到完全一致的哈希映射。
- [ ] `table.json` 的固定版本、来源和再分发许可。
- [ ] AI、Jev 和四个旧题库的请求格式、超时和错误返回是否仍可用；这些属于现场依赖，不能只用单元测试宣称通过。
- [ ] 后端停止时，脚本是否能按预期回退到 GM 配置和旧请求链。
- [ ] 当前前端构建产物是否存在 `static/assets`，以及 `/settings` 路由刷新时 SPA fallback 是否正常。
- [ ] 迁移完成后再重新测量脚本字节数、浏览器解析耗时和页面首屏影响。

## 8. 不在本方案内

- 不迁移 DOM 自动化、视频播放、iframe 轮询和考试页面控制。
- 不在没有样本验证的情况下重写 Typr 或更换字体哈希算法。
- 不以“删除代码行数”作为唯一成功标准；功能回归和后端不可用时的可恢复性优先。
