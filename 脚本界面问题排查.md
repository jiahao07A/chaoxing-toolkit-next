# 脚本界面无法显示 - 解决方案

## 🔍 问题诊断

### 步骤 1: 检查 Tampermonkey 是否启用脚本

1. 点击浏览器工具栏的 **Tampermonkey 图标**（猴子图标）
2. 查看脚本列表中是否有：
   - **💯【超星学习通满分助手】**
3. 确认脚本状态：
   - ✅ 应该显示**绿色对勾**（已启用）
   - ❌ 如果是**红色叉号**（已禁用），点击启用

### 步骤 2: 检查脚本是否在当前页面生效

1. 在学习通页面，点击 Tampermonkey 图标
2. 应该看到：
   ```
   运行中的脚本：
   💯【超星学习通满分助手】
   ```
3. 如果没有显示，说明脚本没有在此页面运行

### 步骤 3: 强制刷新页面

按 **Ctrl + F5** 强制刷新页面（清除缓存）

### 步骤 4: 检查浏览器控制台

1. 按 **F12** 打开开发者工具
2. 切换到 **Console（控制台）** 标签
3. 查看是否有红色错误信息
4. 截图发给我，我可以帮您分析

## 🔧 解决方案

### 方案 1: 重新安装脚本

1. 打开 Tampermonkey 管理面板
2. 找到 **💯【超星学习通满分助手】**
3. 点击 **删除**
4. 重新打开 `学习通脚本.js` 文件
5. 复制全部内容
6. 在 Tampermonkey 中点击 **+** 创建新脚本
7. 粘贴内容并保存
8. 刷新学习通页面

### 方案 2: 检查脚本匹配规则

1. 打开 Tampermonkey 管理面板
2. 点击脚本名称进入编辑
3. 检查 `@match` 行：
   ```javascript
   // @match        *://*.chaoxing.com/*
   ```
4. 确保当前页面 URL 包含 `chaoxing.com`

### 方案 3: 手动添加权限

1. 编辑脚本
2. 确认以下行存在：
   ```javascript
   // @grant        GM_addStyle
   // @grant        GM_getResourceText
   // @grant        GM_getValue
   // @grant        GM_setValue
   // @grant        GM_xmlhttpRequest
   // @grant        unsafeWindow
   ```
3. 保存并刷新页面

### 方案 4: 清除 Tampermonkey 缓存

1. 打开 Tampermonkey 设置
2. 找到 **实用工具** 标签
3. 点击 **清除脚本存储**
4. 刷新页面

## 🎯 验证脚本是否正常工作

脚本正常加载后，您应该看到：

### 在页面右下角
- **齿轮图标** ⚙️ （设置按钮）
- **瞄准图标** 🎯 （功能按钮）

### 在视频播放页面
- 左侧会出现**控制面板**
- 显示当前状态日志

## 📸 需要提供的信息

如果问题仍未解决，请提供：

1. **Tampermonkey 截图**
   - 打开 Tampermonkey 图标
   - 截图脚本列表

2. **控制台截图**
   - 按 F12
   - 切换到 Console
   - 截图错误信息

3. **当前页面 URL**
   - 复制完整的地址栏 URL

## 🔄 临时替代方案

如果脚本界面无法显示，您可以：

### 1. 使用浏览器控制台手动配置

按 F12，在控制台输入：

```javascript
// 启用自定义题库
GM_setValue('config', {
  ...GM_getValue('config'),
  customApiEnabled: true,
  customApiUrl: 'http://localhost:8002/api/search',
  customApiKey: '',
  autoAnswer: true,
  autoSubmit: true
});

// 刷新页面
location.reload();
```

### 2. 直接编辑脚本默认配置

1. 打开 Tampermonkey 编辑脚本
2. 找到 `defaultConfig$1` 部分
3. 修改：
   ```javascript
   customApiEnabled: true,
   customApiUrl: "http://localhost:8002/api/search",
   ```
4. 保存并刷新

## ⚡ 快速检查清单

- [ ] Tampermonkey 扩展已安装
- [ ] 脚本已启用（绿色对勾）
- [ ] 脚本在当前页面运行
- [ ] 已强制刷新页面（Ctrl+F5）
- [ ] 控制台无红色错误
- [ ] Chrome 工作区正确（Default）
- [ ] 题库服务器已启动（http://localhost:8002）

---

请先按照这些步骤检查，然后告诉我遇到了什么情况，我会继续帮您解决！
